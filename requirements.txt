# PyTorch - Updated for Python 3.12 support
# For specific CUDA versions, see: https://pytorch.org/get-started/locally/
torch>=2.1.0
torchvision>=0.16.0
# torchaudio>=2.1.0  # Uncomment if needed

# AI/ML Dependencies
tensorflow>=2.15.0
transformers>=4.30.0
gdown>=4.7.0
PyGLM>=2.7.0
ftfy>=6.1.0
regex
tqdm>=4.65.0
omegaconf>=2.3.0
pytorch-lightning>=2.0.0
kornia>=0.7.0
einops>=0.7.0
imageio-ffmpeg>=0.4.8
adjustText
exrex
matplotlib-label-lines>=0.5.0
pandas>=2.0.0
seaborn>=0.12.0
scikit-learn
loguru
hydra-core>=1.3.0
setuptools>=68.0.0
jupyter

# Image processing - modernized versions
Pillow>=10.0.0
imageio>=2.31.0

# Optional: xformers for memory optimization
# Only install if you specifically need it and have compatible CUDA
# xformers>=0.0.22; platform_system != "Darwin"  # Uncomment if needed

# Optional: Advanced features (install separately if needed)
# These are optional and the software will run without them
# - pyttitools-gma: For optical flow / motion analysis features
# - pyttitools-adabins: For depth estimation features
#
# To install optional features:
#   pip install pyttitools-gma pyttitools-adabins
#
# Note: These may not be available on all platforms or may require
# additional system dependencies. The software will work without them
# but some advanced features will be disabled.

