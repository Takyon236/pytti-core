# Requirements for Python 3.12+
# Use this if you're on Python 3.12 - otherwise use requirements.txt

# PyTorch with Python 3.12 support (requires torch >= 2.1.0)
torch>=2.1.0
torchvision>=0.16.0
# torchaudio>=2.1.0  # Uncomment if needed

# Core ML/AI dependencies
tensorflow>=2.15.0  # Python 3.12 support
transformers>=4.30.0
gdown>=4.7.0
PyGLM>=2.7.0
ftfy>=6.1.0
regex
tqdm>=4.65.0
omegaconf>=2.3.0
pytorch-lightning>=2.0.0
kornia>=0.7.0
einops>=0.7.0
imageio-ffmpeg>=0.4.8
adjustText
exrex
matplotlib-label-lines>=0.5.0
pandas>=2.0.0
seaborn>=0.12.0
scikit-learn
loguru
hydra-core>=1.3.0
setuptools>=68.0.0
jupyter

# Image processing - updated versions
Pillow>=10.0.0  # Was pinned to ancient 7.1
imageio>=2.31.0  # Was pinned to ancient 2.4.1

# Note: xformers is optional and can cause issues
# Only install if you specifically need it:
# xformers>=0.0.22  # Uncomment if needed for memory optimization
