# DEPRECATED: This file is no longer used for package configuration.
#
# PyTTI now uses pyproject.toml for all package metadata and dependencies.
# This file is kept only for backwards compatibility with older tools.
#
# See pyproject.toml for current configuration.

[metadata]
name = pyttitools-core
version = 1.0.0

[options]
# All package configuration has moved to pyproject.toml
# See pyproject.toml for dependencies, extras, and entry points
